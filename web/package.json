{
  "name": "@ppoker/web-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "pnpm --filter @ppoker/site run dev",
    "wasm:generate": "pnpm --filter @ppoker/web-client run wasm:generate",
    "client:prepare": "pnpm --filter @ppoker/web-client run build",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "pnpm run client:prepare && pnpm run typecheck:workspace",
    "typecheck:workspace": "pnpm --recursive --if-present run typecheck",
    "declarations": "pnpm --filter @ppoker/web-client run declarations",
    "test": "pnpm run client:prepare && pnpm run test:workspace",
    "test:e2e": "pnpm --filter @ppoker/site run test:e2e",
    "test:workspace": "pnpm --recursive --if-present run test",
    "coverage": "pnpm --filter @ppoker/web-client run coverage",
    "test:wasm": "pnpm --filter @ppoker/web-client run test:wasm",
    "test:wasm:live": "pnpm --filter @ppoker/web-client run test:wasm:live",
    "build": "pnpm --recursive --if-present run build",
    "package:verify": "pnpm --filter @ppoker/web-client run package:verify",
    "check": "pnpm --filter @ppoker/web-client run check"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.7.0",
    "globals": "17.7.0",
    "prettier": "3.9.5",
    "typescript": "5.9.3",
    "typescript-eslint": "8.64.0"
  }
}
